This project contains a Minecraft Fabric mod that adds recipes and custom blocks for stuffed animals.

## Compatibility

The mod has compatibility with different versions of Minecraft separated out onto different branches.
Each branch is API-compatible with a different range of Minecraft versions.

## Building

The project contains a script, .build_all.sh, which can build .jar files for all supported versions.
